import { ContainerUI } from "@/components/Layout/ContainerUI"
import { FormDadosBase } from "./Components/FormDadosBase"

export const DadosBase: React.FC = () => {
    return (
        <ContainerUI>
            <FormDadosBase/>
        </ContainerUI>
    )
}